{
  "service": {
    "name": "bind",
    "check": {
      "notes": "Check to determine named is healthy",
      "script": "/usr/bin/sudo /usr/bin/namedctl status -n",
      "interval": "30m"
    }
  }
}
